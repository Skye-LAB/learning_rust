fn main() {
    // integer
    let int: u8 = 255;
    let int: u32 = 4294967295;

    // floating point
    let float_point = 5.0;
    let float_point2: f32 = 10.0;

    // operasi aritmatika
    // penjumlahan
    let sum = 5 + 10;

    // pengurangan
    let difference = 95.5 - 4.3;

    // perkalian
    let product = 4 * 30;

    // pembagian
    let quotient = 56.7 / 32.2;
    let floored = 2 / 3; // Results in 0

    // modulus
    let remainder = 43 % 5;

    // boolean
    let t = true;
    let f: bool = false;

    // char
    let c = 'z';


    // tuple
    let tup: (i32, f64, u8) = (500, 6.4, 1);
}
